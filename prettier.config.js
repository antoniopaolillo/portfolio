// @ts-check
/* eslint-env node */

/**
 * Configurações do Prettier.
 * @type {import('prettier').Options}
 */
const configuration = {
  trailingComma: 'all',
  semi: true,
  singleQuote: true,
  tabWidth: 2,
  useTabs: false,
  printWidth: 80
};

module.exports = configuration;
